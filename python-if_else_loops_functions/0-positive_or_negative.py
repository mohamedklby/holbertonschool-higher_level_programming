#!/usr/bin/python3
import random
number = random.randint(-10, 10)
# YOUR CODE HERE

    if number > 0:i
        print("{number} is positive", format (number)); #si number est plus grand que zérmt on afiiche qu'il est positive.

    elif number == 0:
        print("{number} is zero", format (number)); #si number est egal a zéro on affiche qu'il est nul.

    else:
        print("{number} is negative", format (number)); #sinon on affiche qu'il est négative.
